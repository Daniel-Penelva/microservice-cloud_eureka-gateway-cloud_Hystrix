server:
  port: 9191

spring:
  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:mem:testPaymentdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
    jpa:
      database-platform: org.hibernate.dialect.H2Dialect
      show-sql: true

# http://localhost:9191/h2-console/